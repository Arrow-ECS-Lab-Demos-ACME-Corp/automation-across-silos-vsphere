---
#- name: Deploy VM from Template
#  import_playbook: playbook-deploy-vm-from-template.yml

- name: debug
  hosts: localhost
  tasks:
    - name: debug
      ansible.builtin.cmd: 
        cmd: ansible-galaxy collection list
      register: output
    - name: output
      debug:
        var: output
